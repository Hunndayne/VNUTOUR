apiVersion: apps/v1
kind: Deployment
metadata:
  name: photo-ai
spec:
  template:
    spec:
      containers:
        - name: photo-ai
          resources:
            requests:
              cpu: 250m
              memory: 384Mi
            limits:
              cpu: "1"
              memory: 1Gi
